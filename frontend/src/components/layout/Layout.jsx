import React from 'react';
import HeaderTest from "../common/HeaderTest.jsx";
import Header from "../common/Header.jsx";

const Layout = () => {
    return (
        <div className="flex flex-col min-h-screen">
            <Header />
        </div>
    );
};

export default Layout;
